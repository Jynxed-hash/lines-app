@AGENTS.md

## Design System

Always read `DESIGN.md` (repo root, visual system) before making any visual or UI decisions. Product premises live in `docs/DESIGN.md` and stay locked. All font choices, colors, spacing, and aesthetic direction are defined in `DESIGN.md`. Do not deviate without explicit user approval. In QA mode, flag any code that doesn't match `DESIGN.md`.
