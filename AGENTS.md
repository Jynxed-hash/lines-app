# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

GitHub host is `lines-app` via `gh`. Before every commit and every pull, run `code-reviewer` and `security-reviewer`. See `memory/github-etiquette.md` and `.cursor/rules/github-etiquette.mdc`.
